spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
#variaveis de ambiente twilio
twilio:
  sid: 123 #${TWILIO_SID}
  key: 123 #${TWILIO_KEY}
  phone:
    from: 123 #${TWILIO_PHONE_FROM}
    to: 123 #${TWILIO_PHONE_TO}
